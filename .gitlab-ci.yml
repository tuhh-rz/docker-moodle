deployment_system:
    only:
        - master
    tags:
        - master
    script:
        - docker build --no-cache -t local/moodle:tmp_$CI_PROJECT_ID.$CI_BUILD_ID .
        - docker tag local/moodle:tmp_$CI_PROJECT_ID.$CI_BUILD_ID docker.rz.tu-harburg.de:5000/moodle:latest
        - docker push docker.rz.tu-harburg.de:5000/moodle:latest
        - docker rmi local/moodle:tmp_$CI_PROJECT_ID.$CI_BUILD_ID